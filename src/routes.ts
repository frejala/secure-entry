/**
 * List of public routes
 * @type {string[]}
 */
export const publicRoutes: string[] = ["/"];

/**
 * List of auth routes
 * @type {string[]}
 */
export const authRoutes: string[] = ["/login", "/register"];
